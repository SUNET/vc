



Network Working Group                                        M. Svensson
Internet-Draft                                                     SUNET
Intended status: Informational                              L. Johansson
Expires: 5 February 2027                                      J. Rangsmo
                                                        Siros Foundation
                                                           4 August 2026


                    OpenID4VP to OIDC Claims Bridge
                draft-svensson-openid4vp-oidc-bridge-00

Abstract

   This document defines a mechanism for bridging OpenID for Verifiable
   Presentations (OpenID4VP) to OpenID Connect (OIDC).  It specifies how
   a verifier that collects credentials from a wallet via OpenID4VP can
   expose those credential claims to Relying Parties as standard OIDC
   claims, enabling existing OIDC deployments to consume digital
   credentials without implementing OpenID4VP directly.

Status of This Memo

   This Internet-Draft is submitted in full conformance with the
   provisions of BCP 78 and BCP 79.

   Internet-Drafts are working documents of the Internet Engineering
   Task Force (IETF).  Note that other groups may also distribute
   working documents as Internet-Drafts.  The list of current Internet-
   Drafts is at https://datatracker.ietf.org/drafts/current/.

   Internet-Drafts are draft documents valid for a maximum of six months
   and may be updated, replaced, or obsoleted by other documents at any
   time.  It is inappropriate to use Internet-Drafts as reference
   material or to cite them other than as "work in progress."

   This Internet-Draft will expire on 5 February 2027.

Copyright Notice

   Copyright (c) 2026 IETF Trust and the persons identified as the
   document authors.  All rights reserved.

   This document is subject to BCP 78 and the IETF Trust's Legal
   Provisions Relating to IETF Documents (https://trustee.ietf.org/
   license-info) in effect on the date of publication of this document.
   Please review these documents carefully, as they describe your rights
   and restrictions with respect to this document.




Svensson, et al.         Expires 5 February 2027                [Page 1]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


Table of Contents

   1.  Introduction  . . . . . . . . . . . . . . . . . . . . . . . .   2
   2.  Architecture Overview . . . . . . . . . . . . . . . . . . . .   3
   3.  Terminology . . . . . . . . . . . . . . . . . . . . . . . . .   3
   4.  OP (Verifier/Bridge) Requirements . . . . . . . . . . . . . .   3
     4.1.  The openid4vp_data Claim  . . . . . . . . . . . . . . . .   4
       4.1.1.  The openid4vp_data Object . . . . . . . . . . . . . .   4
       4.1.2.  Credential Entry Object . . . . . . . . . . . . . . .   5
     4.2.  Discovery . . . . . . . . . . . . . . . . . . . . . . . .   5
     4.3.  Authentication Flow . . . . . . . . . . . . . . . . . . .   6
     4.4.  Credential Mapping  . . . . . . . . . . . . . . . . . . .   6
   5.  RP (Relying Party) Requirements . . . . . . . . . . . . . . .   6
     5.1.  Requesting Credential Claims  . . . . . . . . . . . . . .   6
     5.2.  Consuming Credential Claims . . . . . . . . . . . . . . .   7
     5.3.  Trust Model . . . . . . . . . . . . . . . . . . . . . . .   7
   6.  Limitations and Considerations  . . . . . . . . . . . . . . .   8
     6.1.  Claim Set Size Limits . . . . . . . . . . . . . . . . . .   8
     6.2.  Error Handling  . . . . . . . . . . . . . . . . . . . . .   8
     6.3.  Scope Mapping . . . . . . . . . . . . . . . . . . . . . .   9
     6.4.  Scope of This Specification . . . . . . . . . . . . . . .   9
   7.  Security Considerations . . . . . . . . . . . . . . . . . . .  10
     7.1.  Credential Replay . . . . . . . . . . . . . . . . . . . .  10
     7.2.  Token Leakage . . . . . . . . . . . . . . . . . . . . . .  10
     7.3.  Claim Injection . . . . . . . . . . . . . . . . . . . . .  10
     7.4.  Privacy Considerations  . . . . . . . . . . . . . . . . .  10
   8.  IANA Considerations . . . . . . . . . . . . . . . . . . . . .  10
     8.1.  JSON Web Token Claims Registration  . . . . . . . . . . .  11
     8.2.  OpenID Connect Discovery Metadata Registration  . . . . .  11
   9.  References  . . . . . . . . . . . . . . . . . . . . . . . . .  11
     9.1.  Normative References  . . . . . . . . . . . . . . . . . .  11
     9.2.  Informative References  . . . . . . . . . . . . . . . . .  12
   Authors' Addresses  . . . . . . . . . . . . . . . . . . . . . . .  12

1.  Introduction

   OpenID for Verifiable Presentations (OpenID4VP) enables wallets to
   present digital credentials to verifiers.  However, many existing
   services rely on OpenID Connect (OIDC) for authentication and
   attribute retrieval, and implementing OpenID4VP represents a
   significant barrier for these Relying Parties.










Svensson, et al.         Expires 5 February 2027                [Page 2]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


   This document specifies how a verifier, acting as an OIDC OpenID
   Provider (OP), can bridge the gap by collecting credentials from a
   wallet via OpenID4VP and then exposing the resulting claims in
   standard OIDC ID Tokens and UserInfo responses.  The Relying Party
   interacts with a normal OIDC flow and receives credential data in the
   "openid4vp_data" claim without needing any knowledge of the
   underlying OpenID4VP protocol.

2.  Architecture Overview

   The following diagram illustrates the high-level interaction between
   the participants:

+--------+                  +------------------+              +--------+
|        |  1. OIDC AuthN   |   OP / Verifier  |  2. OpenID4VP|        |
|   RP   | ---------------> |     (Bridge)     | ------------>| Wallet |
|        |                  |                  | <------------|        |
|        |                  |                  |  3. VP Token |        |
|        |                  |                  |              +--------+
|        |                  |  4. Verify &     |
|        |  5. ID Token +   |     extract      |
|        |  openid4vp_data  |     claims       |
|        | <--------------- |                  |
+--------+                  +------------------+

  Trust in credential issuers is established out of band
  (pre-configured issuer keys, trust frameworks, status lists).

Flow:
  1. RP sends OIDC Authentication Request to OP (with credential
     type scopes).
  2. OP initiates OpenID4VP request to user's wallet.
  3. Wallet presents VP Token with disclosed credentials.
  4. OP verifies credentials (signature, expiry, revocation status)
     and extracts claims.
  5. OP returns ID Token / UserInfo containing openid4vp_data to RP.

3.  Terminology

   The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT",
   "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this
   document are to be interpreted as described in [RFC2119].

4.  OP (Verifier/Bridge) Requirements







Svensson, et al.         Expires 5 February 2027                [Page 3]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


4.1.  The openid4vp_data Claim

   The following is a non-normative example of an OIDC ID Token
   containing the "openid4vp_data" claim with two presented credentials:

   {
     "iss": "https://issuer.example.org",
     "sub": "user@example.org",
     "iat": 1722772800,
     "exp": 1722859200,
     "openid4vp_data": {
       "credentials": {
         "ehic": {
           "format": "vc+sd-jwt",
           "vct": "https://credentials.example.org/ehic",
           "claims": {
             "name": "John Doe",
             "dob": "1990-01-01",
             "ehic_number": "1234567890"
           }
         },
         "pda1": {
           "format": "vc+sd-jwt",
           "vct": "https://credentials.example.org/pda1",
           "claims": {
             "name": "John Doe",
             "dob": "1990-01-01",
             "pda1_number": "0987654321"
           }
         }
       }
     }
   }

4.1.1.  The openid4vp_data Object

   The "openid4vp_data" claim is a top-level JSON object included in the
   OIDC ID Token or UserInfo response.  It contains credential data
   obtained via an OpenID4VP presentation flow, re-packaged for
   consumption by OIDC Relying Parties.

   The object MUST contain the following member:

   credentials
      A JSON object where each key is a scope value corresponding to the
      requested credential type and each value is a Credential Entry
      object as defined in Section 4.1.2.  There MUST be at least one
      entry.  Keys MUST be unique within the object.



Svensson, et al.         Expires 5 February 2027                [Page 4]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


   Additional members MAY be present.  Implementations that do not
   recognise additional members MUST ignore them.

4.1.2.  Credential Entry Object

   Each Credential Entry object represents a single credential presented
   during the OpenID4VP flow.  It MUST contain the following member:

   claims
      A JSON object containing the disclosed claims from the credential.
      Each key is a claim name and each value is the claim value.  Claim
      names are determined by the credential type and MUST be strings.
      Claim values MAY be any valid JSON type.

   A Credential Entry object MAY contain the following additional
   members:

   format
      A string indicating the original credential format (e.g., "vc+sd-
      jwt", "mso_mdoc").  If omitted, the format is unspecified.

   vct
      A string containing the Verifiable Credential Type identifier, as
      defined in the credential's metadata.

   Additional members MAY be present.  Implementations that do not
   recognise additional members MUST ignore them.

4.2.  Discovery

   An OP that supports this bridge mechanism MUST include
   "openid4vp_data" in the "claims_supported" list in its OpenID Connect
   Discovery metadata document.

   The OP SHOULD also advertise the credential types it can collect by
   listing the corresponding scopes in the "scopes_supported" discovery
   metadata field (e.g., "ehic", "pda1").

   The OP MAY additionally include a "openid4vp_credentials_supported"
   member in its discovery metadata.  The value is a JSON array of
   objects, each containing at minimum a "scope" string and a "vct"
   string, providing RPs with a machine-readable mapping between scopes
   and credential types.








Svensson, et al.         Expires 5 February 2027                [Page 5]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


4.3.  Authentication Flow

   When the OP receives an OIDC Authentication Request that includes a
   request for "openid4vp_data" claims (via the "claims" parameter or a
   registered scope), it MUST:

   1.  Initiate an OpenID4VP presentation request to the user's wallet
       for the requested credential types.

   2.  Verify the presented credentials according to the applicable
       trust framework.

   3.  Extract the disclosed claims from each verified credential.

   4.  Construct the "openid4vp_data" object as defined in
       Section 4.1.1.

   5.  Include the "openid4vp_data" claim in the ID Token, the UserInfo
       response, or both, depending on the OP's policy and the size
       considerations described in Section 6.1.

4.4.  Credential Mapping

   The OP MUST use the scope value as the key within the "credentials"
   object.  For example, if the RP requested scope "ehic", the resulting
   entry MUST be keyed as "ehic".  This ensures a predictable, stable
   mapping between the RP's request and the response.

   The OP MUST NOT include claims that were not disclosed by the wallet.
   The OP MUST NOT modify claim values during the mapping.

5.  RP (Relying Party) Requirements

5.1.  Requesting Credential Claims

   An RP that wishes to receive credential data via this bridge MUST
   include the appropriate credential type scopes in the OIDC
   Authentication Request.  Each scope corresponds to a credential type
   (e.g., "ehic", "pda1").  The presence of any credential type scope
   signals to the OP that the "openid4vp_data" claim is requested.

   The requested scopes directly correlate to the keys in the
   "credentials" object returned by the OP.  For example, if the RP
   requests scopes "ehic" and "pda1", the OP will return an
   "openid4vp_data" claim with corresponding "ehic" and "pda1" entries
   in the "credentials" object.





Svensson, et al.         Expires 5 February 2027                [Page 6]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


   The RP MAY additionally use the "claims" request parameter to specify
   which individual claims within a credential type are desired,
   enabling selective disclosure.

   The following is a non-normative example of a claims request:

   {
     "id_token": {
       "openid4vp_data": {
         "essential": true,
         "credentials": {
           "ehic": {
             "claims": ["name", "dob", "ehic_number"]
           }
         }
       }
     }
   }

5.2.  Consuming Credential Claims

   The "openid4vp_data" claim structure is defined in Section 4.1.  The
   RP MUST parse the claim according to that definition.  Specifically,
   the RP MUST:

   1.  Check for the presence of the "openid4vp_data" claim.  If the
       claim was requested as essential and is absent, the RP SHOULD
       treat the authentication as failed.

   2.  Parse the "credentials" object and extract the entries relevant
       to its use case.

   3.  Validate that the expected claims are present in each Credential
       Entry's "claims" object.

   The RP MUST NOT assume that all requested scopes will be present in
   the response; the user may have declined to present certain
   credentials.  The RP MUST ignore unrecognised credential keys and
   unrecognised members within Credential Entry objects.

5.3.  Trust Model

   The RP places trust in the OP to have correctly verified the
   presented credentials.  The RP does not interact with the wallet or
   credential issuer directly.  The trust relationship between the RP
   and the OP is established through standard OIDC mechanisms (client
   registration, token validation, TLS).




Svensson, et al.         Expires 5 February 2027                [Page 7]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


6.  Limitations and Considerations

6.1.  Claim Set Size Limits

   OIDC ID Tokens are typically passed as JWTs in HTTP headers or URL
   fragments, which impose practical size limits.  Browser URL length
   limits are commonly around 2048 bytes, and many HTTP servers reject
   headers exceeding 8192 bytes.  When multiple credentials with many
   disclosed claims are included in the "openid4vp_data" object, the
   resulting token may exceed these limits.

   Implementations SHOULD consider the following mitigations:

   *  Return credential claims via the UserInfo endpoint rather than
      embedding them directly in the ID Token.

   *  Limit the number of disclosed claims to those requested by the
      Relying Party via the OIDC "claims" parameter.

   *  Use token introspection or reference tokens where supported by the
      deployment.

6.2.  Error Handling

   The OP MUST handle the following failure scenarios gracefully:

   Wallet rejection
      The user declines to present credentials.  The OP MAY either omit
      the "openid4vp_data" claim entirely or return an OIDC error
      response (e.g., "access_denied") depending on whether the
      credential presentation was essential to the authentication.

   Wallet timeout
      The wallet does not respond within a reasonable time.  The OP
      SHOULD treat this as equivalent to a rejection.

   Invalid credentials
      The wallet presents credentials that fail verification (expired,
      revoked, untrusted issuer).  The OP MUST NOT include unverified
      credential claims in the "openid4vp_data" object.  The OP MAY
      proceed without those credentials or fail the authentication.

   Partial presentation
      The wallet presents only a subset of the requested credentials.
      The OP MUST include only the successfully verified credentials and
      MUST NOT fabricate entries for missing credentials.





Svensson, et al.         Expires 5 February 2027                [Page 8]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


6.3.  Scope Mapping

   An OP MAY define custom OIDC scopes that map to specific credential
   presentation requests.  For example, the scope "ehic" might trigger a
   request for an EHIC credential with a predefined set of claims.

   When using scope-based mapping, the OP SHOULD document the mapping
   between scopes and credential types in its discovery metadata or out-
   of-band documentation.  The OP MUST ensure that the scope semantics
   are stable and do not change unexpectedly for registered RPs.

6.4.  Scope of This Specification

   This specification defines the data format of the "openid4vp_data"
   claim, the mechanism for requesting and returning credential claims
   via OIDC, and the responsibilities of the OP and RP in that exchange.
   The following aspects are explicitly out of scope:

   *  *Credential validation policy.* This specification does not define
      which issuers to trust, which revocation mechanisms to check, or
      what trust frameworks to apply.  These decisions are deployment-
      specific and determined by the OP operator.

   *  *Business logic and authorization decisions.* How the RP
      interprets the received claims — for example, whether an EHIC
      credential grants access to a healthcare service, or whether a
      PDA1 is sufficient for a given transaction — is entirely the RP's
      responsibility and outside this specification.

   *  *Credential issuance.* This specification deals only with
      presentation of existing credentials, not with the issuance of new
      credentials (which is covered by OpenID4VCI).

   *  *Wallet implementation.* The interaction between the OP and the
      wallet follows the OpenID4VP protocol.  This specification does
      not impose additional requirements on wallet implementations.

   *  *User consent and identity matching.* How the OP associates a
      credential presentation with an OIDC subject identifier, and how
      user consent is obtained, are implementation details left to the
      OP.

   In summary, this specification provides the plumbing for transporting
   verified credential claims through OIDC.  Everything above that layer
   — trust decisions, access control, and business rules — is the domain
   of the deploying parties.





Svensson, et al.         Expires 5 February 2027                [Page 9]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


7.  Security Considerations

7.1.  Credential Replay

   The OP MUST ensure that credential presentations are bound to the
   current authentication session.  The OP SHOULD use nonces in the
   OpenID4VP request to prevent replay of previously captured VP Tokens.

   The RP MUST validate standard JWT claims ("iat", "exp", "nonce") in
   the ID Token to ensure freshness of the "openid4vp_data" claim.

7.2.  Token Leakage

   ID Tokens containing "openid4vp_data" may carry sensitive personal
   data (e.g., national ID numbers, health information).
   Implementations MUST use TLS for all token transmissions.  The OP
   SHOULD prefer delivering credential claims via the UserInfo endpoint
   (which uses a back-channel request) rather than embedding them in the
   ID Token (which may be exposed in browser history or logs).

7.3.  Claim Injection

   The OP MUST NOT allow external parties to inject or modify claims
   within the "openid4vp_data" object.  The OP MUST populate this claim
   exclusively from verified credential presentations.  The ID Token
   MUST be signed by the OP to protect integrity.

7.4.  Privacy Considerations

   The bridge architecture introduces the OP as a party that observes
   all credential claims presented by the user.  This creates a
   correlation point.  Deployments SHOULD consider the following:

   *  The OP SHOULD request only the minimum claims necessary to satisfy
      the RP's request (selective disclosure).

   *  The OP SHOULD NOT log or persist credential claims beyond what is
      necessary for the authentication session.

   *  RPs SHOULD be aware that repeated presentations of the same
      credential claims across sessions may enable correlation by the
      OP.

   *  Where possible, deployments SHOULD use pairwise subject
      identifiers to limit cross-RP correlation.

8.  IANA Considerations




Svensson, et al.         Expires 5 February 2027               [Page 10]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


8.1.  JSON Web Token Claims Registration

   This specification requests registration of the following claim in
   the IANA "JSON Web Token Claims" registry:

   Claim Name
      openid4vp_data

   Claim Description
      Digital credential claims obtained via an OpenID4VP presentation,
      structured for consumption by OIDC Relying Parties.

   Change Controller
      IETF

   Specification Document(s)
      Section 3.1 of this document

8.2.  OpenID Connect Discovery Metadata Registration

   This specification requests registration of the following metadata
   parameter:

   Metadata Name
      openid4vp_credentials_supported

   Metadata Description
      A JSON array describing the credential types the OP can collect
      via OpenID4VP and expose as OIDC claims.

   Change Controller
      IETF

   Specification Document(s)
      Section 4.1 of this document

9.  References

9.1.  Normative References

   [RFC2119]  Bradner, S., "Key words for use in RFCs to Indicate
              Requirement Levels", BCP 14, RFC 2119, March 1997,
              <https://www.rfc-editor.org/info/rfc2119>.

   [OpenID.Core]
              Sakimura, N., Bradley, J., Jones, M., de Medeiros, B., and
              C. Mortimore, "OpenID Connect Core 1.0", November 2014,
              <https://openid.net/specs/openid-connect-core-1_0.html>.



Svensson, et al.         Expires 5 February 2027               [Page 11]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


   [OpenID.Discovery]
              Sakimura, N., Bradley, J., Jones, M., and E. Jay, "OpenID
              Connect Discovery 1.0", November 2014,
              <https://openid.net/specs/openid-connect-discovery-
              1_0.html>.

   [OpenID4VP]
              Terbu, O., Lodderstedt, T., Yasuda, K., and T. Looker,
              "OpenID for Verifiable Presentations (OpenID4VP) 1.0",
              2024, <https://openid.net/specs/openid-4-verifiable-
              presentations-1_0.html>.

   [RFC8259]  Bray, T., "The JavaScript Object Notation (JSON) Data
              Interchange Format", STD 90, RFC 8259, December 2017,
              <https://www.rfc-editor.org/info/rfc8259>.

9.2.  Informative References

   [RFC7519]  Jones, M., Bradley, J., and N. Sakimura, "JSON Web Token
              (JWT)", RFC 7519, May 2015,
              <https://www.rfc-editor.org/info/rfc7519>.

   [SD-JWT.VC]
              Terbu, O. and D. Fett, "SD-JWT-based Verifiable
              Credentials (SD-JWT VC)", 2024,
              <https://www.ietf.org/archive/id/draft-ietf-oauth-sd-jwt-
              vc-05.html>.

   [ISO.18013-5]
              ISO/IEC, "Personal identification — ISO-compliant driving
              licence — Part 5: Mobile driving licence (mDL)
              application", 2021,
              <https://www.iso.org/standard/69084.html>.

Authors' Addresses

   Magnus Svensson
   SUNET
   Email: masv@sunet.se


   Leif Johansson
   Siros Foundation
   Email: leifj@siros.org


   Joel Rangsmo
   Siros Foundation



Svensson, et al.         Expires 5 February 2027               [Page 12]

Internet-Draft          OpenID4VP to OIDC Bridge             August 2026


   Email: joel@siros.org


















































Svensson, et al.         Expires 5 February 2027               [Page 13]
