<?xml version="1.0" encoding="utf-8"?>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude"
     ipr="trust200902"
     docName="draft-svensson-credential-oidc-bridge-00"
     category="info"
     submissionType="independent"
     version="3">

  <front>
    <title abbrev="Credential to OIDC Bridge">Credential Presentation to OIDC Claims Bridge</title>

    <seriesInfo name="Internet-Draft" value="draft-svensson-credential-oidc-bridge-00"/>

    <author initials="M." surname="Svensson" fullname="Magnus Svensson">
      <organization>SUNET</organization>
      <address>
        <email>masv@sunet.se</email>
      </address>
    </author>

    <author initials="L." surname="Johansson" fullname="Leif Johansson">
      <organization>Siros Foundation</organization>
      <address>
        <email>leifj@siros.org</email>
      </address>
    </author>

    <author initials="J." surname="Rangsmo" fullname="Joel Rangsmo">
      <organization>Siros Foundation</organization>
      <address>
        <email>joel@siros.org</email>
      </address>
    </author>

    <date year="2026" month="August" day="4"/>

    <abstract>
      <t>This document defines a mechanism for conveying digital
      credential claims via OpenID Connect (OIDC). It specifies how
      an OpenID Provider (OP) that collects credentials from a wallet
      can expose those claims to Relying Parties as standard OIDC
      claims, enabling existing OIDC deployments to consume digital
      credentials without implementing any wallet-facing presentation
      protocol.</t>
    </abstract>
  </front>

  <middle>
    <section anchor="introduction">
      <name>Introduction</name>
      <t>Digital credential wallets can present verified claims to
      verifiers using protocols such as OpenID for Verifiable
      Presentations (OpenID4VP) <xref target="OpenID4VP"/> or
      DIDComm. However, many existing services rely on OpenID Connect
      (OIDC) <xref target="OpenID.Core"/> for authentication and
      attribute retrieval, and implementing a wallet-facing
      presentation protocol represents a significant barrier for
      these Relying Parties.</t>

      <t>This document specifies how an OP, acting as a bridge, can
      collect credentials from a wallet using any suitable
      presentation protocol and expose the resulting claims in
      standard OIDC ID Tokens and UserInfo responses. The Relying
      Party interacts with a normal OIDC flow and receives credential
      data in the "presented_credentials" claim without needing any
      knowledge of the underlying presentation protocol.</t>

      <t>This specification is intended to complement, not compete
      with, work produced by the OpenID Foundation. Implementers
      are encouraged to follow developments in the OpenID Foundation's
      Digital Credentials Protocols working group.</t>
    </section>

    <section anchor="architecture">
      <name>Architecture Overview</name>
      <t>The following diagram illustrates the high-level interaction
      between the participants:</t>

      <artwork type="ascii-art"><![CDATA[
+------+                +----------------+            +--------+
|      |  1. OIDC AuthN |       OP       | 2.Present  |        |
|  RP  | -------------> |    (Bridge)    | ---------->| Wallet |
|      |                |                | <----------|        |
|      |                |                | 3.Response |        |
|      |                |                |            +--------+
|      |                | 4. Verify &    |
|      |  5. ID Token   |    extract     |
|      |                |    claims      |
|      | <------------- |                |
+------+                +----------------+

The presentation protocol (step 2-3) is out of scope.
The OP may use OpenID4VP, DIDComm, or any other mechanism.

Flow:
 1. RP sends OIDC AuthN Request to OP (credential scopes).
 2. OP requests credential presentation from wallet.
 3. Wallet responds with disclosed credentials.
 4. OP verifies credentials and extracts claims.
 5. OP returns ID Token/UserInfo with presented_credentials.
]]></artwork>
    </section>

    <section anchor="terminology">
      <name>Terminology</name>
      <t>The key words "<bcp14>MUST</bcp14>", "<bcp14>MUST NOT</bcp14>",
      "<bcp14>REQUIRED</bcp14>", "<bcp14>SHALL</bcp14>",
      "<bcp14>SHALL NOT</bcp14>", "<bcp14>SHOULD</bcp14>",
      "<bcp14>SHOULD NOT</bcp14>", "<bcp14>RECOMMENDED</bcp14>",
      "<bcp14>MAY</bcp14>", and "<bcp14>OPTIONAL</bcp14>" in this
      document are to be interpreted as described in BCP 14
      <xref target="RFC2119"/> <xref target="RFC8174"/> when, and
      only when, they appear in all capitals, as shown here.</t>
    </section>

    <section anchor="op-requirements">
      <name>OP (Verifier/Bridge) Requirements</name>

      <section anchor="format">
        <name>The presented_credentials Claim</name>
        <t>The following is a non-normative example of an OIDC ID Token
        containing the "presented_credentials" claim with a credential
        set containing two credentials:</t>

        <sourcecode type="json"><![CDATA[
{
  "iss": "https://issuer.example.org",
  "sub": "user@example.org",
  "iat": 1722772800,
  "exp": 1722859200,
  "presented_credentials": [
    {
      "ehic": [
        {
          "format": "vc+sd-jwt",
          "vct": "https://credentials.example.org/ehic",
          "claims": {
            "name": "John Doe",
            "dob": "1990-01-01",
            "ehic_number": "1234567890"
          }
        }
      ],
      "pda1": [
        {
          "format": "vc+sd-jwt",
          "vct": "https://credentials.example.org/pda1",
          "claims": {
            "name": "John Doe",
            "dob": "1990-01-01",
            "pda1_number": "0987654321"
          }
        }
      ]
    }
  ]
}
]]></sourcecode>

        <section anchor="presented-credentials-object">
          <name>The presented_credentials Array</name>
          <t>The "presented_credentials" claim is a top-level JSON
          array included in the OIDC ID Token or UserInfo response.
          It contains credential data obtained via a credential
          presentation flow (e.g., OpenID4VP, DIDComm), re-packaged
          for consumption by OIDC Relying Parties.</t>

          <t>The array contains one or more Credential Set objects.
          Each Credential Set is a JSON object where each key is a
          scope value corresponding to a credential type and each
          value is a JSON array of Credential Entry objects as
          defined in <xref target="credential-entry"/>.</t>

          <t>Within a Credential Set:</t>
          <ul>
            <li>Each key <bcp14>MUST</bcp14> be unique.</li>
            <li>Each value (array) <bcp14>MUST</bcp14> contain at
            least one Credential Entry. Multiple entries indicate
            the wallet presented more than one credential of that
            type.</li>
          </ul>

          <t>The outer array <bcp14>MUST</bcp14> contain at least
          one Credential Set. In most deployments a single set is
          returned; multiple sets are possible when the RP requested
          alternatives (see <xref target="rp-requesting"/>).</t>

          <t>Additional members within a Credential Set
          <bcp14>MAY</bcp14> be present. Implementations that do
          not recognise additional members <bcp14>MUST</bcp14>
          ignore them.</t>
        </section>

        <section anchor="credential-entry">
          <name>Credential Entry Object</name>
          <t>Each Credential Entry object represents a single credential
          presented during the presentation flow. It <bcp14>MUST</bcp14>
          contain the following member:</t>

          <dl newline="true">
            <dt>claims</dt>
            <dd>A JSON object <xref target="RFC8259"/> containing the
            disclosed claims from the
            credential. Each key is a claim name and each value is the
            claim value. Claim names are determined by the credential
            type and <bcp14>MUST</bcp14> be strings. Claim values
            <bcp14>MAY</bcp14> be any valid JSON type.</dd>
          </dl>

          <t>A Credential Entry object <bcp14>MAY</bcp14> contain the
          following additional members:</t>

          <dl newline="true">
            <dt>format</dt>
            <dd>A string indicating the original credential format
            (e.g., "vc+sd-jwt", "mso_mdoc"). If omitted, the format
            is unspecified.</dd>

            <dt>vct</dt>
            <dd>A string containing the Verifiable Credential Type
            identifier, as defined in the credential's metadata.</dd>
          </dl>

          <t>Additional members <bcp14>MAY</bcp14> be present.
          Implementations that do not recognise additional members
          <bcp14>MUST</bcp14> ignore them.</t>
        </section>
      </section>

      <section anchor="op-discovery">
        <name>Discovery</name>
        <t>An OP that supports this bridge mechanism <bcp14>MUST</bcp14>
        include "presented_credentials" in the "claims_supported" list in its
        OpenID Connect Discovery <xref target="OpenID.Discovery"/>
        metadata document.</t>

        <t>The OP <bcp14>SHOULD</bcp14> also advertise the credential
        types it can collect by listing the corresponding scopes in
        the "scopes_supported" discovery metadata field (e.g.,
        "ehic", "pda1").</t>

        <t>The OP <bcp14>MAY</bcp14> additionally include a
        "presented_credentials_supported" member in its discovery
        metadata. The value is a JSON array of objects, each containing
        at minimum a "scope" string and a "vct" string, providing
        RPs with a machine-readable mapping between scopes and
        credential types.</t>
      </section>

      <section anchor="op-authn-flow">
        <name>Authentication Flow</name>
        <t>When the OP receives an OIDC Authentication Request that
        includes a request for credentials (via the "requested_credentials"
        claims parameter or a registered scope), it <bcp14>MUST</bcp14>:</t>

        <ol>
          <li>Initiate a credential presentation request to the
          user's wallet for the requested credential types, using
          the presentation protocol supported by the deployment.</li>
          <li>Verify the presented credentials according to the
          applicable trust framework.</li>
          <li>Extract the disclosed claims from each verified
          credential.</li>
          <li>Construct the "presented_credentials" object as defined in
          <xref target="presented-credentials-object"/>.</li>
          <li>Include the "presented_credentials" claim in the ID Token,
          the UserInfo response, or both, depending on the OP's
          policy and the size considerations described in
          <xref target="claim-size-limits"/>.</li>
        </ol>

        <t>Common presentation protocols include OpenID4VP
        <xref target="OpenID4VP"/> and DIDComm Present Proof.
        The choice of protocol is a deployment decision and does not
        affect the "presented_credentials" format returned to the
        RP.</t>
      </section>

      <section anchor="op-credential-mapping">
        <name>Credential Mapping</name>
        <t>The OP <bcp14>MUST</bcp14> use the scope value as the key
        within the "credentials" object. For example, if the RP
        requested scope "ehic", the resulting entry
        <bcp14>MUST</bcp14> be keyed as "ehic". This ensures a
        predictable, stable mapping between the RP's request and
        the response.</t>

        <t>The OP <bcp14>MUST NOT</bcp14> include claims that were
        not disclosed by the wallet. The OP <bcp14>MUST NOT</bcp14>
        modify claim values during the mapping.</t>

        <t>The OP is responsible for translating the RP's OIDC-level
        request (scopes, claims parameter) into a protocol-specific
        credential query directed at the wallet. For example, when
        using OpenID4VP the OP would construct a DCQL (Digital
        Credentials Query Language) query matching the requested
        credential types; when using DIDComm the OP would build a
        Present Proof request. This translation is
        deployment-specific and outside the scope of this
        specification. The OP <bcp14>MUST</bcp14> document the
        mapping between OIDC scopes and the credential types they
        resolve to, either in its discovery metadata or in
        out-of-band documentation.</t>

        <t>The OIDC request model (scopes and the "claims" parameter)
        is intentionally simpler than the query languages available
        in presentation protocols (e.g., DCQL in OpenID4VP). This
        means that certain constraints expressible in a presentation
        query — such as issuer restrictions, issuance date filters,
        or compound field requirements — cannot be communicated by
        the RP. The OP <bcp14>MUST</bcp14> apply its own policy to
        resolve these edge cases (for example, by selecting the most
        recent matching credential or by restricting accepted issuers
        via configuration). The OP <bcp14>SHOULD</bcp14> document
        any such policies so that RPs can anticipate the resulting
        behaviour.</t>

        <t>The structure of the "presented_credentials" claim
        <bcp14>MUST</bcp14> be independent of the credential
        presentation protocol used between the OP and the wallet.
        Whether the OP collected credentials via OpenID4VP, DIDComm,
        or any other mechanism, the resulting claim format
        <bcp14>MUST</bcp14> conform to this specification. The RP
        <bcp14>MUST NOT</bcp14> need to be aware of which
        presentation protocol was used.</t>
      </section>
    </section>

    <section anchor="rp-requirements">
      <name>RP (Relying Party) Requirements</name>

      <section anchor="rp-requesting">
        <name>Requesting Credential Claims</name>
        <t>An RP that wishes to receive credential data via this
        bridge <bcp14>MUST</bcp14> include the appropriate credential
        type scopes in the OIDC Authentication Request. Each scope
        corresponds to a credential type (e.g., "ehic", "pda1").
        The presence of any credential type scope signals to the OP
        that the "requested_credentials" claim parameter applies.</t>

        <t>The requested scopes directly correlate to the keys in
        credential sets returned by the OP. For example, if the RP
        requests scopes "ehic" and "pda1", the OP will return a
        credential set containing "ehic" and "pda1" entries.</t>

        <t>The RP <bcp14>MAY</bcp14> additionally use the "claims"
        request parameter to specify which individual claims within
        a credential type are desired, enabling selective
        disclosure.</t>

        <t>The RP <bcp14>MAY</bcp14> use the "credential_sets"
        structure within the "requested_credentials" claims request
        parameter to express combinatorial logic over credential
        types:</t>

        <ul>
          <li><strong>AND (within a set):</strong> Credentials
          listed in the same credential set with "essential": true
          are all required. The OP <bcp14>MUST</bcp14> fail the
          authentication if any essential credential in the set
          cannot be obtained.</li>
          <li><strong>OR (between sets):</strong> Multiple
          credential sets represent alternatives. The OP
          <bcp14>MUST</bcp14> attempt to satisfy the sets in order
          and use the first set that can be fully satisfied. Only
          one credential set is returned in the response.</li>
        </ul>

        <t>The following is a non-normative example requesting PID
        AND EHIC together (both are required):</t>

        <sourcecode type="json"><![CDATA[
{
  "id_token": {
    "requested_credentials": {
      "credential_sets": [
        {
          "pid": { "essential": true, "claims": ["name"] },
          "ehic": { "essential": true, "claims": ["ehic_number"] }
        }
      ]
    }
  }
}
]]></sourcecode>

        <t>The following is a non-normative example requesting PID
        OR EHIC (either one satisfies the RP). The OP tries the
        first set; if the wallet cannot provide a PID, it falls
        back to the second set:</t>

        <sourcecode type="json"><![CDATA[
{
  "id_token": {
    "requested_credentials": {
      "credential_sets": [
        {
          "pid": { "essential": true, "claims": ["name"] }
        },
        {
          "ehic": { "essential": true, "claims": ["ehic_number"] }
        }
      ]
    }
  }
}
]]></sourcecode>

        <t>The following is a non-normative example requesting PID
        (required) with EHIC as optional (nice-to-have):</t>

        <sourcecode type="json"><![CDATA[
{
  "id_token": {
    "requested_credentials": {
      "credential_sets": [
        {
          "pid": { "essential": true, "claims": ["name"] },
          "ehic": { "essential": false, "claims": ["ehic_number"] }
        }
      ]
    }
  }
}
]]></sourcecode>

        <t>If any credential marked essential is missing from the
        wallet's response and no alternative set can be satisfied,
        the OP <bcp14>MUST</bcp14> return an OIDC error response
        (e.g., "access_denied") rather than a partial credential
        set.</t>

        <t>If the wallet presents multiple credentials of the same
        type (e.g., two EHICs for different family members), the OP
        returns all of them in the array. The RP is responsible for
        selecting the appropriate credential by inspecting the
        returned claims (for example, matching on name or date of
        birth against the authenticated user's identity).</t>
      </section>

      <section anchor="rp-consuming">
        <name>Consuming Credential Claims</name>
        <t>The "presented_credentials" claim structure is defined in
        <xref target="format"/>. The RP <bcp14>MUST</bcp14> parse
        the claim according to that definition. Specifically, the RP
        <bcp14>MUST</bcp14>:</t>

        <ol>
          <li>Check for the presence of the "presented_credentials" claim.
          If the claim was requested as essential and is absent, the
          RP <bcp14>SHOULD</bcp14> treat the authentication as
          failed.</li>
          <li>Parse the "credentials" object and extract the entries
          relevant to its use case.</li>
          <li>Validate that the expected claims are present in each
          Credential Entry's "claims" object.</li>
        </ol>

        <t>The RP <bcp14>MUST NOT</bcp14> assume that all requested
        scopes will be present in the response; the user may have
        declined to present certain credentials. The RP
        <bcp14>MUST</bcp14> ignore unrecognised credential keys and
        unrecognised members within Credential Entry objects.</t>
      </section>

      <section anchor="rp-trust">
        <name>Trust Model</name>
        <t>The RP places trust in the OP to have correctly verified
        the presented credentials. The RP does not interact with the
        wallet or credential issuer directly. The trust relationship
        between the RP and the OP is established through standard OIDC
        mechanisms (client registration, token validation, TLS).</t>
      </section>
    </section>

    <section anchor="security-considerations">
      <name>Limitations and Considerations</name>

      <section anchor="claim-size-limits">
        <name>Claim Set Size Limits</name>
        <t>OIDC ID Tokens are typically passed as JWTs
        <xref target="RFC7519"/> in HTTP headers
        or URL fragments, which impose practical size limits. Browser
        URL length limits are commonly around 2048 bytes, and many HTTP
        servers reject headers exceeding 8192 bytes. When multiple
        credentials with many disclosed claims are included in the
        "presented_credentials" object, the resulting token may exceed these
        limits.</t>

        <t>Implementations <bcp14>SHOULD</bcp14> consider the following
        mitigations:</t>

        <ul>
          <li>Return credential claims via the UserInfo endpoint rather
          than embedding them directly in the ID Token.</li>
          <li>Limit the number of disclosed claims to those requested
          by the Relying Party via the OIDC "claims" parameter.</li>
          <li>Use token introspection or reference tokens where
          supported by the deployment.</li>
        </ul>
      </section>

      <section anchor="error-handling">
        <name>Error Handling</name>
        <t>The OP <bcp14>MUST</bcp14> handle the following failure
        scenarios gracefully:</t>

        <dl newline="true">
          <dt>Wallet rejection</dt>
          <dd>The user declines to present credentials. The OP
          <bcp14>MAY</bcp14> either omit the "presented_credentials" claim
          entirely or return an OIDC error response
          (e.g., "access_denied") depending on whether the credential
          presentation was essential to the authentication.</dd>

          <dt>Wallet timeout</dt>
          <dd>The wallet does not respond within a reasonable time.
          The OP <bcp14>SHOULD</bcp14> treat this as equivalent to a
          rejection.</dd>

          <dt>Invalid credentials</dt>
          <dd>The wallet presents credentials that fail verification
          (expired, revoked, untrusted issuer). The OP
          <bcp14>MUST NOT</bcp14> include unverified credential claims
          in the "presented_credentials" object. The OP <bcp14>MAY</bcp14>
          proceed without those credentials or fail the
          authentication.</dd>

          <dt>Partial presentation</dt>
          <dd>The wallet presents only a subset of the requested
          credentials. The OP <bcp14>MUST</bcp14> include only the
          successfully verified credentials and <bcp14>MUST NOT</bcp14>
          fabricate entries for missing credentials.</dd>
        </dl>
      </section>

      <section anchor="scope-mapping">
        <name>Scope Mapping</name>
        <t>An OP <bcp14>MAY</bcp14> define custom OIDC scopes that map
        to specific credential presentation requests. For example, the
        scope "ehic" might trigger a request for an EHIC credential
        with a predefined set of claims.</t>

        <t>When using scope-based mapping, the OP <bcp14>SHOULD</bcp14>
        document the mapping between scopes and credential types in its
        discovery metadata or out-of-band documentation. The OP
        <bcp14>MUST</bcp14> ensure that the scope semantics are stable
        and do not change unexpectedly for registered RPs.</t>
      </section>

      <section anchor="scope-of-spec">
        <name>Scope of This Specification</name>
        <t>This specification defines the data format of the
        "presented_credentials" claim, the mechanism for requesting and
        returning credential claims via OIDC, and the responsibilities
        of the OP and RP in that exchange. The following aspects are
        explicitly out of scope:</t>

        <ul>
          <li><strong>Credential validation policy.</strong> This
          specification does not define which issuers to trust, which
          revocation mechanisms to check, or what trust frameworks to
          apply. These decisions are deployment-specific and determined
          by the OP operator.</li>
          <li><strong>Business logic and authorization decisions.</strong>
          How the RP interprets the received claims — for example,
          whether an EHIC credential grants access to a healthcare
          service, or whether a PDA1 is sufficient for a given
          transaction — is entirely the RP's responsibility and outside
          this specification.</li>
          <li><strong>Credential issuance.</strong> This specification
          deals only with presentation of existing credentials, not with
          the issuance of new credentials (which is covered by
          OpenID4VCI).</li>
          <li><strong>Wallet implementation.</strong> The interaction
          between the OP and the wallet uses a presentation protocol
          such as OpenID4VP or DIDComm. This specification does not
          mandate a specific protocol and does not impose additional
          requirements on wallet implementations.</li>
          <li><strong>User consent and identity matching.</strong> How
          the OP associates a credential presentation with an OIDC
          subject identifier, and how user consent is obtained, are
          implementation details left to the OP.</li>
        </ul>

        <t>In summary, this specification provides the plumbing for
        transporting verified credential claims through OIDC. Everything
        above that layer — trust decisions, access control, and business
        rules — is the domain of the deploying parties.</t>
      </section>
    </section>

    <section anchor="impl-status">
      <name>Implementation Status</name>
      <t>This section records the status of known implementations of
      the protocol defined by this specification at the time of
      posting of this Internet-Draft, and is based on a proposal
      described in <xref target="RFC7942"/>. The description of
      implementations in this section is intended to assist the IETF
      in its decision processes in progressing drafts to RFCs.</t>

      <section anchor="impl-sunet">
        <name>SUNET Verifiable Credentials Platform</name>
        <dl newline="true">
          <dt>Organization</dt>
          <dd>SUNET (Swedish University Computer Network)</dd>

          <dt>Implementation</dt>
          <dd>Open-source verifiable credentials platform including
          issuer, verifier, and wallet components with an API gateway
          implementing the OpenID4VP-to-OIDC bridge described in this
          document.</dd>

          <dt>Description</dt>
          <dd>The implementation supports SD-JWT VC
          <xref target="SD-JWT.VC"/> and mdoc
          <xref target="ISO.18013-5"/> credential
          formats, exposes the "presented_credentials" claim via both ID Token
          and UserInfo endpoint, and has been deployed in production for
          European higher-education credential use cases (EHIC, PDA1,
          micro-credentials).</dd>

          <dt>Maturity</dt>
          <dd>Production</dd>

          <dt>Coverage</dt>
          <dd>Implements all normative requirements of this
          specification.</dd>

          <dt>Contact</dt>
          <dd>masv@sunet.se</dd>

          <dt>Source Code</dt>
          <dd>https://github.com/SUNET/vc (open source, BSD-2-Clause)</dd>
        </dl>
      </section>
    </section>

    <section anchor="security">
      <name>Security Considerations</name>

      <section anchor="sec-replay">
        <name>Credential Replay</name>
        <t>The OP <bcp14>MUST</bcp14> ensure that credential
        presentations are bound to the current authentication session.
        The OP <bcp14>SHOULD</bcp14> use nonces in the OpenID4VP
        request to prevent replay of previously captured VP Tokens.</t>

        <t>The RP <bcp14>MUST</bcp14> validate standard JWT claims
        ("iat", "exp", "nonce") in the ID Token to ensure freshness
        of the "presented_credentials" claim.</t>
      </section>

      <section anchor="sec-token-leakage">
        <name>Token Leakage</name>
        <t>ID Tokens containing "presented_credentials" may carry sensitive
        personal data (e.g., national ID numbers, health information).
        Implementations <bcp14>MUST</bcp14> use TLS for all token
        transmissions. The OP <bcp14>SHOULD</bcp14> prefer delivering
        credential claims via the UserInfo endpoint (which uses a
        back-channel request) rather than embedding them in the ID Token
        (which may be exposed in browser history or logs).</t>
      </section>

      <section anchor="sec-claim-injection">
        <name>Claim Injection</name>
        <t>The OP <bcp14>MUST NOT</bcp14> allow external parties to
        inject or modify claims within the "presented_credentials" object.
        The OP <bcp14>MUST</bcp14> populate this claim exclusively
        from verified credential presentations. The ID Token
        <bcp14>MUST</bcp14> be signed by the OP to protect integrity.</t>
      </section>

      <section anchor="sec-privacy">
        <name>Privacy Considerations</name>
        <t>The bridge architecture introduces the OP as a party that
        observes all credential claims presented by the user. This
        creates a correlation point. Deployments <bcp14>SHOULD</bcp14>
        consider the following:</t>

        <ul>
          <li>The OP <bcp14>SHOULD</bcp14> request only the minimum
          claims necessary to satisfy the RP's request (selective
          disclosure).</li>
          <li>The OP <bcp14>SHOULD NOT</bcp14> log or persist
          credential claims beyond what is necessary for the
          authentication session.</li>
          <li>RPs <bcp14>SHOULD</bcp14> be aware that repeated
          presentations of the same credential claims across sessions
          may enable correlation by the OP.</li>
          <li>Where possible, deployments <bcp14>SHOULD</bcp14> use
          pairwise subject identifiers to limit cross-RP
          correlation.</li>
        </ul>
      </section>
    </section>

    <section anchor="iana">
      <name>IANA Considerations</name>

      <t>The claim name "presented_credentials" was chosen to be
      protocol-agnostic, clearly describing the content (credentials
      that were presented) without implying a dependency on any
      particular presentation protocol or namespace.</t>

      <section anchor="iana-claim-reg">
        <name>JSON Web Token Claims Registration</name>
        <t>This specification requests registration of the following
        claim in the IANA "JSON Web Token Claims" registry:</t>

        <dl newline="true">
          <dt>Claim Name</dt>
          <dd>presented_credentials</dd>

          <dt>Claim Description</dt>
          <dd>Digital credential claims obtained via an OpenID4VP
          presentation, structured for consumption by OIDC Relying
          Parties.</dd>

          <dt>Change Controller</dt>
          <dd>IETF</dd>

          <dt>Specification Document(s)</dt>
          <dd>Section 3.1 of this document</dd>
        </dl>
      </section>

      <section anchor="iana-discovery-reg">
        <name>OpenID Connect Discovery Metadata Registration</name>
        <t>This specification requests registration of the following
        metadata parameter:</t>

        <dl newline="true">
          <dt>Metadata Name</dt>
          <dd>presented_credentials_supported</dd>

          <dt>Metadata Description</dt>
          <dd>A JSON array describing the credential types the OP can
          collect via OpenID4VP and expose as OIDC claims.</dd>

          <dt>Change Controller</dt>
          <dd>IETF</dd>

          <dt>Specification Document(s)</dt>
          <dd>Section 4.1 of this document</dd>
        </dl>
      </section>
    </section>
  </middle>

  <back>
    <references>
      <name>References</name>

      <references>
        <name>Normative References</name>

        <reference anchor="RFC2119" target="https://www.rfc-editor.org/info/rfc2119">
          <front>
            <title>Key words for use in RFCs to Indicate Requirement Levels</title>
            <author initials="S." surname="Bradner" fullname="S. Bradner"/>
            <date year="1997" month="March"/>
          </front>
          <seriesInfo name="BCP" value="14"/>
          <seriesInfo name="RFC" value="2119"/>
        </reference>

        <reference anchor="RFC8174" target="https://www.rfc-editor.org/info/rfc8174">
          <front>
            <title>Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words</title>
            <author initials="B." surname="Leiba" fullname="B. Leiba"/>
            <date year="2017" month="May"/>
          </front>
          <seriesInfo name="BCP" value="14"/>
          <seriesInfo name="RFC" value="8174"/>
        </reference>

        <reference anchor="OpenID.Core" target="https://openid.net/specs/openid-connect-core-1_0.html">
          <front>
            <title>OpenID Connect Core 1.0</title>
            <author initials="N." surname="Sakimura" fullname="Nat Sakimura"/>
            <author initials="J." surname="Bradley" fullname="John Bradley"/>
            <author initials="M." surname="Jones" fullname="Michael Jones"/>
            <author initials="B." surname="de Medeiros" fullname="Breno de Medeiros"/>
            <author initials="C." surname="Mortimore" fullname="Chuck Mortimore"/>
            <date year="2014" month="November"/>
          </front>
        </reference>

        <reference anchor="OpenID.Discovery" target="https://openid.net/specs/openid-connect-discovery-1_0.html">
          <front>
            <title>OpenID Connect Discovery 1.0</title>
            <author initials="N." surname="Sakimura" fullname="Nat Sakimura"/>
            <author initials="J." surname="Bradley" fullname="John Bradley"/>
            <author initials="M." surname="Jones" fullname="Michael Jones"/>
            <author initials="E." surname="Jay" fullname="Edmund Jay"/>
            <date year="2014" month="November"/>
          </front>
        </reference>

        <reference anchor="OpenID4VP" target="https://openid.net/specs/openid-4-verifiable-presentations-1_0.html">
          <front>
            <title>OpenID for Verifiable Presentations (OpenID4VP) 1.0</title>
            <author initials="O." surname="Terbu" fullname="Oliver Terbu"/>
            <author initials="T." surname="Lodderstedt" fullname="Torsten Lodderstedt"/>
            <author initials="K." surname="Yasuda" fullname="Kristina Yasuda"/>
            <author initials="T." surname="Looker" fullname="Tobias Looker"/>
            <date year="2024"/>
          </front>
        </reference>

        <reference anchor="RFC8259" target="https://www.rfc-editor.org/info/rfc8259">
          <front>
            <title>The JavaScript Object Notation (JSON) Data Interchange Format</title>
            <author initials="T." surname="Bray" fullname="T. Bray"/>
            <date year="2017" month="December"/>
          </front>
          <seriesInfo name="STD" value="90"/>
          <seriesInfo name="RFC" value="8259"/>
        </reference>
      </references>

      <references>
        <name>Informative References</name>

        <reference anchor="RFC7519" target="https://www.rfc-editor.org/info/rfc7519">
          <front>
            <title>JSON Web Token (JWT)</title>
            <author initials="M." surname="Jones" fullname="Michael Jones"/>
            <author initials="J." surname="Bradley" fullname="John Bradley"/>
            <author initials="N." surname="Sakimura" fullname="Nat Sakimura"/>
            <date year="2015" month="May"/>
          </front>
          <seriesInfo name="RFC" value="7519"/>
        </reference>

        <reference anchor="SD-JWT.VC" target="https://www.ietf.org/archive/id/draft-ietf-oauth-sd-jwt-vc-05.html">
          <front>
            <title>SD-JWT-based Verifiable Credentials (SD-JWT VC)</title>
            <author initials="O." surname="Terbu" fullname="Oliver Terbu"/>
            <author initials="D." surname="Fett" fullname="Daniel Fett"/>
            <date year="2024"/>
          </front>
        </reference>

        <reference anchor="ISO.18013-5" target="https://www.iso.org/standard/69084.html">
          <front>
            <title>Personal identification — ISO-compliant driving licence — Part 5: Mobile driving licence (mDL) application</title>
            <author>
              <organization>ISO/IEC</organization>
            </author>
            <date year="2021"/>
          </front>
        </reference>

        <reference anchor="RFC7942" target="https://www.rfc-editor.org/info/rfc7942">
          <front>
            <title>Improving Awareness of Running Code: The Implementation Status Section</title>
            <author initials="Y." surname="Sheffer" fullname="Y. Sheffer"/>
            <author initials="A." surname="Farrel" fullname="A. Farrel"/>
            <date year="2016" month="July"/>
          </front>
          <seriesInfo name="BCP" value="205"/>
          <seriesInfo name="RFC" value="7942"/>
        </reference>
      </references>
    </references>
  </back>
</rfc>
